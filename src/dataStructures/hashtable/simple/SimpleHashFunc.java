package dataStructures.hashtable.simple;

import dataStructures.hashtable.HashFunctionAPI;

public class SimpleHashFunc implements HashFunctionAPI {

    @Override
    public int hashFunc(String key) {
        return 0;
    }
}
